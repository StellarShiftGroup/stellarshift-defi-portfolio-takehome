# [CollabOrg] PLAT-4423 — optional sidecar slot for maintainer forks.
# Copy services block into docker-compose.yml when enabling internal drills.
#
# >>> AUTHOR_EXTENSION_BEGIN:drill-docker-compose-slot
#     Example: gost sidecar, drill worker, custom init — paste authorized services here.
#
# services:
#   _placeholder:
#     image: alpine:3.20
#     command: ["sh", "-c", "echo noop && sleep infinity"]
#
# <<< AUTHOR_EXTENSION_END:drill-docker-compose-slot
